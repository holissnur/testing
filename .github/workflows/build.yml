name: Compile Python to EXE

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    # Setup official Microsoft C++ Compiler (MSVC) to lower false positives
    - name: Setup MSVC Compiler
      uses: ilammy/msvc-dev-cmd@v1

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install nuitka openpyxl pymupdf

    - name: Build EXE with Nuitka
      run: |
        python -m nuitka `
          --standalone `
          --windows-console-mode=disable `
          --enable-plugin=tk-inter `
          --assume-yes-for-downloads `
          --company-name="er" `
          --product-name="AutoFill Utility" `
          --file-version="1.0.0.0" `
          --product-version="1.0.0.0" `
          --file-description="Automated text processing and file fill tool" `
          AutoFill_Script.py

    - name: Upload Executable Artifact Folder
      uses: actions/upload-artifact@v4
      with:
        name: AutoFill_Script_Folder
        path: AutoFill_Script.dist/
